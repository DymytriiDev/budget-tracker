# Authentication token for API access
AUTH_TOKEN=your-secret-token-here

# Neon Postgres connection string (auto-set by Vercel when using Neon integration)
POSTGRES_URL=postgresql://user:password@host/database
